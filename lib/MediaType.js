// enum to use when requesing a particular media from DB
//each media must set its type, for now only home type exists used in home screen.

var types = {
	Home: { value: 'home', label: 'Accueil' }
}

exports = module.exports = types;
